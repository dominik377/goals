import {FundingLevelItem} from "./fundingLevelItem";

export class FundingLevels {
  moneyOneTime = new FundingLevelItem('moneyOneTime');
  moneyPereniall = new FundingLevelItem('moneyPerennial');
  timeOneTime = new FundingLevelItem('timeOneTime');
  timePerennial = new FundingLevelItem('timePerennial');
}
